# MUI Material Symbols

This repository contains the [Material Symbols](https://fonts.google.com/icons) with the weight {{weight}} in the style {{style}} for [Material UI](https://mui.com/material-ui).

## Getting Started

Install this package via npm:

```bash
npm install @aivot/mui-material-symbols-{{weight}}-{{style}}
```

## Usage
Use these symbols like any other Material UI icon:

```tsx
import SpaSymbol from '@aivot/mui-material-symbols-{{weight}}-{{style}}/spa/Spa';
import SpaFilledSymbol from '@aivot/mui-material-symbols-{{weight}}-{{style}}/spa/SpaFilled';

function MyComponent() {
  return (
    <div>
      <SpaSymbol />
      <SpaFilledSymbol />
    </div>
  );
}
```
